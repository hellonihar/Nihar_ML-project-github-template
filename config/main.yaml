# Project Configuration

data:
  raw_path: "data/raw"
  processed_path: "data/processed"

model:
  name: "random_forest"
  params:
    n_estimators: 100
    max_depth: 10

training:
  test_size: 0.2
  random_state: 42
